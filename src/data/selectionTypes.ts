// Classes
import SelectionType from "../classes/SelectionType";

const selectionTypes: SelectionType[] = [
    new SelectionType({ label: "Chord", value: "chord" }),
    new SelectionType({ label: "Scale", value: "scale" })
];

export default selectionTypes;
